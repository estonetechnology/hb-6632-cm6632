Audio20HighSpeedConfigDscr:
      db   DSCR_CONFIG_LEN   ;; Descriptor length
      db   DSCR_CONFIG       ;; Descriptor type
      db   (Audio20HighSpeedConfigDscrEnd-Audio20HighSpeedConfigDscr) mod 256 ;; Total Length (LSB)
      db   (Audio20HighSpeedConfigDscrEnd-Audio20HighSpeedConfigDscr)  /  256 ;; Total Length (MSB)
#ifdef _MIDI_      
      db   10                ;; Number of interfaces
#else
      db   8                 ;; Number of interfaces
#endif
      db   1                 ;; Configuration number
      db   0                 ;; Configuration string
#ifdef _REMOTE_WAKEUP_      
      db   11100000b         ;; Attributes (b7 - reserved, b6 - selfpwr, b5 - rwu)
#else
      db   11000000b         ;; Attributes (b7 - reserved, b6 - selfpwr, b5 - rwu)
#endif      
      db   50                ;; Power requirement (div 2 ma)

;; High Speed Interface association Descriptor
      db   DSCR_INTRFC_ASSOC_LEN ;; Descriptor length
      db   DSCR_INTRFC_ASSOC     ;; Descriptor type
      db   0                     ;; First interface
      db   7                     ;; Number of interfaces
      db   1                     ;; Function class
      db   0                     ;; Function sub class
      db   20H                   ;; Function protocol
      db   0                     ;; String index

;; High Speed Audio Control Interface 0 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   0                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   01H               ;; Interface class
      db   01H               ;; Interface sub class
      db   20H               ;; Interface procotol
      db   0                 ;; Interface descriptor string index

HsAcInterfaceDscrHeader:
      db   DSCR_AC_HDR_LEN   ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_HEADER ;; AC Descriptor subtype
      dw   0002H             ;; Audio class release number
      db   0AH               ;; Category
      db   (HsAcInterfaceDscrEnd-HsAcInterfaceDscrHeader) mod 256 ;; Total Length (LSB)
      db   (HsAcInterfaceDscrEnd-HsAcInterfaceDscrHeader)  /  256 ;; Total Length (MSB)
      db   0                 ;; Control bitmap

;; High Speed Input Terminal 1 Descriptor
      db   DSCR_AC_IT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_IT     ;; AC Descriptor subtype
      db   1                 ;; ID
      dw   0101H             ;; Terminal type: USB stream
      db   0                 ;; Associate terminal
      db   18                ;; Clock source ID
      db   0                 ;; Number of channels
      dw   0000H             ;; Config of logical channels
      dw   0000H             ;; Config of logical channels
      db   0                 ;; String index of first logical channel
      dw   4000H             ;; Control bitmap: Support cluster control
      db   0                 ;; String index of IT

;; High Speed Input Terminal 2 Descriptor
      db   DSCR_AC_IT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_IT     ;; AC Descriptor subtype
      db   2                 ;; ID
      dw   0101H             ;; Terminal type: USB stream
      db   0                 ;; Associate terminal
      db   19                ;; Clock source ID
      db   0                 ;; Number of channels
      dw   0000H             ;; Config of logical channels
      dw   0000H             ;; Config of logical channels
      db   0                 ;; String index of first logical channel
      dw   4000H             ;; Control bitmap: Support cluster control
      db   0                 ;; String index of IT

;; High Speed Input Terminal 3 Descriptor
      db   DSCR_AC_IT_LEN_20 ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_IT     ;; AC Descriptor subtype
      db   3                 ;; ID
      dw   0101H             ;; Terminal type: USB stream
      db   0                 ;; Associate terminal
      db   20                ;; Clock source ID
      db   0                 ;; Number of channels
      dw   0000H             ;; Config of logical channels
      dw   0000H             ;; Config of logical channels
      db   0                 ;; String index of first logical channel
      dw   4000H             ;; Control bitmap: Support cluster control
      db   0                 ;; String index of IT

;; High Speed Input Terminal 4 Descriptor
      db   DSCR_AC_IT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_IT     ;; AC Descriptor subtype
      db   4                 ;; ID
      dw   0102H             ;; Terminal type: Microphone
      db   0                 ;; Associate terminal
      db   21                ;; Clock source ID
      db   02H               ;; Number of channels
      dw   0300H             ;; Config of logical channels
      dw   0000H             ;; Config of logical channels
      db   0                 ;; String index of first logical channel
      dw   4400H             ;; Control bitmap: Support connector, cluster control
      db   0                 ;; String index of IT

;; High Speed Input Terminal 5 Descriptor
      db   DSCR_AC_IT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_IT     ;; AC Descriptor subtype
      db   5                 ;; ID
      dw   0306H             ;; Terminal type: Line connector
      db   0                 ;; Associate terminal
      db   22                ;; Clock source ID
      db   02H               ;; Number of channels
      dw   0300H             ;; Config of logical channels
      dw   0000H             ;; Config of logical channels
      db   0                 ;; String index of first logical channel
      dw   4400H             ;; Control bitmap: Support connector, cluster control
      db   0                 ;; String index of IT

;; High Speed Input Terminal 6 Descriptor
      db   DSCR_AC_IT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_IT     ;; AC Descriptor subtype
      db   6                 ;; ID
      dw   0506H             ;; Terminal type: SPDIF
      db   0                 ;; Associate terminal
      db   23                ;; Clock source ID
      db   02H               ;; Number of channels
      dw   0300H             ;; Config of logical channels
      dw   0000H             ;; Config of logical channels
      db   0                 ;; String index of first logical channel
      dw   4400H             ;; Control bitmap: Support connector, cluster control
      db   0                 ;; String index of IT

;; High Speed Output Terminal 7 Descriptor
      db   DSCR_AC_OT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_OT     ;; AC Descriptor subtype
      db   7                 ;; ID
      dw   0103H             ;; Terminal type: Speaker
      db   0                 ;; Associate terminal
      db   13                ;; Source ID
      db   18                ;; Clock source ID
      dw   0400H             ;; Control bitmap: Support connector control
      db   0                 ;; String index of OT

;; High Speed Output Terminal 8 Descriptor
      db   DSCR_AC_OT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_OT     ;; AC Descriptor subtype
      db   8                 ;; ID
      dw   0506H             ;; Terminal type: SPDIF
      db   0                 ;; Associate terminal
      db   14                ;; Source ID
      db   19               ;; Clock source ID
      dw   0400H             ;; Control bitmap: Support connector control
      db   0                 ;; String index of OT

;; High Speed Output Terminal 9 Descriptor
      db   DSCR_AC_OT_LEN_20 ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_OT     ;; AC Descriptor subtype
      db   9                 ;; ID
      dw   0203H             ;; Terminal type: Headphone
      db   0                 ;; Associate terminal
      db   15                ;; Source ID
      db   20                ;; Clock source ID
      dw   0400H             ;; Control bitmap: Support connector control
      db   0                 ;; String index of OT

;; High Speed Output Terminal 10 Descriptor
      db   DSCR_AC_OT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_OT     ;; AC Descriptor subtype
      db   10                ;; ID
      dw   0101H             ;; Terminal type: USB stream
      db   0                 ;; Associate terminal
      db   16                ;; Source ID
      db   21                ;; Clock source ID
      dw   0000H             ;; Control bitmap
      db   0                 ;; String index of OT

;; High Speed Output Terminal 11 Descriptor
      db   DSCR_AC_OT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_OT     ;; AC Descriptor subtype
      db   11                ;; ID
      dw   0101H             ;; Terminal type: USB stream
      db   0                 ;; Associate terminal
      db   17                ;; Source ID
      db   22                ;; Clock source ID
      dw   0000H             ;; Control bitmap
      db   0                 ;; String index of OT

;; High Speed Output Terminal 12 Descriptor
      db   DSCR_AC_OT_LEN_20    ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_OT     ;; AC Descriptor subtype
      db   12                ;; ID
      dw   0101H             ;; Terminal type: USB stream
      db   0                 ;; Associate terminal
      db   6                 ;; Source ID
      db   23                ;; Clock source ID
      dw   0000H             ;; Control bitmap
      db   0                 ;; String index of OT

Audio20HsFeature13Dscr:
      db   Audio20HsFeature13DscrEnd-Audio20HsFeature13Dscr;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_FEATURE;; AC Descriptor subtype
      db   13                ;; ID
      db   1                 ;; Source ID
      dw   0300H             ;; Control byte of channel 0
      dw   0000H             ;; Control byte of channel 0
      dw   0C00H             ;; Control byte of channel 1
      dw   0000H             ;; Control byte of channel 1
      dw   0C00H             ;; Control byte of channel 2
      dw   0000H             ;; Control byte of channel 2

#ifdef _MODEL_CM6632A_

      dw   0C00H             ;; Control byte of channel 3
      dw   0000H             ;; Control byte of channel 3
      dw   0C00H             ;; Control byte of channel 4
      dw   0000H             ;; Control byte of channel 4
      dw   0C00H             ;; Control byte of channel 5
      dw   0000H             ;; Control byte of channel 5
      dw   0C00H             ;; Control byte of channel 6
      dw   0000H             ;; Control byte of channel 6
      dw   0C00H             ;; Control byte of channel 7
      dw   0000H             ;; Control byte of channel 7
      dw   0C00H             ;; Control byte of channel 8
      dw   0000H             ;; Control byte of channel 8

#endif

      db   0                 ;; String index of Feature
Audio20HsFeature13DscrEnd:

Audio20HsFeature14Dscr:
      db   Audio20HsFeature14DscrEnd-Audio20HsFeature14Dscr;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_FEATURE;; AC Descriptor subtype
      db   14                ;; ID
      db   2                 ;; Source ID
      dw   0300H             ;; Control byte of channel 0
      dw   0000H             ;; Control byte of channel 0
      dw   0000H             ;; Control byte of channel 1
      dw   0000H             ;; Control byte of channel 1
      dw   0000H             ;; Control byte of channel 2
      dw   0000H             ;; Control byte of channel 2
      db   0                 ;; String index of Feature
Audio20HsFeature14DscrEnd:

Audio20HsFeature15Dscr:
      db   Audio20HsFeature15DscrEnd-Audio20HsFeature15Dscr;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_FEATURE;; AC Descriptor subtype
      db   15                ;; ID
      db   3                 ;; Source ID
      dw   0300H             ;; Control byte of channel 0
      dw   0000H             ;; Control byte of channel 0
      dw   0C00H             ;; Control byte of channel 1
      dw   0000H             ;; Control byte of channel 1
      dw   0C00H             ;; Control byte of channel 2
      dw   0000H             ;; Control byte of channel 2
      db   0                 ;; String index of Feature
Audio20HsFeature15DscrEnd:

Audio20HsFeature16Dscr:
      db   Audio20HsFeature16DscrEnd-Audio20HsFeature16Dscr ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_FEATURE;; AC Descriptor subtype
      db   16                ;; ID
      db   4                 ;; Source ID
      dw   0330H             ;; Control byte of channel 0
      dw   0000H             ;; Control byte of channel 0
      dw   0C00H             ;; Control byte of channel 1
      dw   0000H             ;; Control byte of channel 1
      dw   0C00H             ;; Control byte of channel 2
      dw   0000H             ;; Control byte of channel 2
      db   0                 ;; String index of Feature
Audio20HsFeature16DscrEnd:

Audio20HsFeature17Dscr:
      db   Audio20HsFeature17DscrEnd-Audio20HsFeature17Dscr ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_FEATURE;; AC Descriptor subtype
      db   17                ;; ID
      db   5                 ;; Source ID
      dw   0300H             ;; Control byte of channel 0
      dw   0000H             ;; Control byte of channel 0
      dw   0000H             ;; Control byte of channel 1
      dw   0000H             ;; Control byte of channel 1
      dw   0000H             ;; Control byte of channel 2
      dw   0000H             ;; Control byte of channel 2
      db   0                 ;; String index of Feature
Audio20HsFeature17DscrEnd:

;; High Speed Clock Source 18 Descriptor
      db   DSCR_AC_CLKSRC_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_CLKSRC ;; AC Descriptor subtype
      db   18                ;; Unit ID
#ifdef _FEEDBACK_		
      db   00000011b         ;; Attributes: Internal programmable clock     
#else
      db   00000111b         ;; Attributes: Internal programmable clock     
#endif		
      db   00000111b         ;; Control bitmap
      db   0                 ;; Associated Terminal
      db   0                 ;; String index of Clock Source
      
;; High Speed Clock Source 19 Descriptor
      db   DSCR_AC_CLKSRC_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_CLKSRC ;; AC Descriptor subtype
      db   19                ;; Unit ID
#ifdef _FEEDBACK_		
      db   00000011b         ;; Attributes: Internal programmable clock     
#else
      db   00000111b         ;; Attributes: Internal programmable clock     
#endif		
      db   00000111b         ;; Control bitmap
      db   0                 ;; Associated Terminal
      db   0                 ;; String index of Clock Source

;; High Speed Clock Source 20 Descriptor
      db   DSCR_AC_CLKSRC_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_CLKSRC ;; AC Descriptor subtype
      db   20                ;; Unit ID
#ifdef _FEEDBACK_		
      db   00000011b         ;; Attributes: Internal programmable clock     
#else
      db   00000111b         ;; Attributes: Internal programmable clock     
#endif		
      db   00000111b         ;; Control bitmap
      db   0                 ;; Associated Terminal
      db   0                 ;; String index of Clock Source
      
;; High Speed Clock Source 21 Descriptor
      db   DSCR_AC_CLKSRC_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_CLKSRC ;; AC Descriptor subtype
      db   21                ;; Unit ID
      db   00000011b         ;; Attributes: Internal programmable clock
      db   00000111b         ;; Control bitmap
      db   0                 ;; Associated Terminal
      db   0                 ;; String index of Clock Source
      
;; High Speed Clock Source 22 Descriptor
      db   DSCR_AC_CLKSRC_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_CLKSRC ;; AC Descriptor subtype
      db   22                ;; Unit ID
      db   00000011b         ;; Attributes: Internal programmable clock
      db   00000111b         ;; Control bitmap
      db   0                 ;; Associated Terminal
      db   0                 ;; String index of Clock Source

;; High Speed Clock Source 23 Descriptor
      db   DSCR_AC_CLKSRC_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_CLKSRC ;; AC Descriptor subtype
      db   23                ;; Unit ID
      db   00000000b         ;; Attributes: External clock
      db   00000111b         ;; Control bitmap
      db   0                 ;; Associated Terminal
      db   0                 ;; String index of Clock Source

HsAcInterfaceDscrEnd:

;; AC Interrupt Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   8FH               ;; Endpoint number, and direction
      db   ET_INT            ;; Endpoint type
      dw   0600H             ;; Maximun packet size
      db   4                 ;; Polling interval: 1 ms
      
;; High Speed Audio Stream Interface 1 (IT 1) alternate 0 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   0                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   4                 ;; Interface descriptor string index
      
;; High Speed Audio Stream Interface 1 (IT 1) alternate 1 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   1                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   2                    ;; Bytes per sample
      db   16                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif      
      
;; High Speed Audio Stream Interface 1 (IT 1) alternate 2 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   2                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H 	                ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif

#ifdef _MODEL_CM6632A_

;; High Speed Audio Stream Interface 1 (IT 1) alternate 3 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   3                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   4                    ;; Number of channels
      dw   3300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   2                    ;; Bytes per sample
      db   16                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   4*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   4*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif      
      
;; High Speed Audio Stream Interface 1 (IT 1) alternate 4 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   4                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H 	                ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   4                    ;; Number of channels
      dw   3300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   4*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   4*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif

;; High Speed Audio Stream Interface 1 (IT 1) alternate 5 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   5                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   6                    ;; Number of channels
      dw   3F00H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   2                    ;; Bytes per sample
      db   16                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   6*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   6*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif      
      
;; High Speed Audio Stream Interface 1 (IT 1) alternate 6 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   6                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H 	                ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   6                    ;; Number of channels
      dw   3F00H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   6*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   6*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif

;; High Speed Audio Stream Interface 1 (IT 1) alternate 7 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   7                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   8                    ;; Number of channels
      dw   3F06H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   2                    ;; Bytes per sample
      db   16                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   8*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   8*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif      
      
;; High Speed Audio Stream Interface 1 (IT 1) alternate 8 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   1                 ;; Zero-based index of this interface
      db   8                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   01H 	                ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   8                    ;; Number of channels
      dw   3F06H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   05H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   8*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   8*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   85H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif

#endif

;; High Speed Audio Stream Interface 2 (IT 2) alternate 0 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   2                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   0                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   5                 ;; Interface descriptor string index
 
;; High Speed Audio Stream Interface 2 (IT 2) alternate 1 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   2                 ;; Zero-based index of this interface
      db   1                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   02H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                 ;; Format type: Type I
      db   2                 ;; Bytes per sample
      db   16                ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   06H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interva: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   86H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif                 
            
;; High Speed Audio Stream Interface 2 (IT 2) alternate 2 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   2                 ;; Zero-based index of this interface
      db   2                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   02H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   06H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   86H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif                 
      
;; High Speed Audio Stream Interface 2 (IT 2) alternate 3 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   2                 ;; Zero-based index of this interface
      db   3                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   02H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   03                   ;; Format type: Type III
      dw   8113H                ;; Data type: AC3 DTS-I DTS-II DTS-III WMA
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   3                  ;; Format type: Type III
      db   2                  ;; Bytes per sample
      db   16                ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   06H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   86H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif                     

;; High Speed Audio Stream Interface 3 (IT 3) alternate 0 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   3                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   0                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   11                ;; Interface descriptor string index
      
;; High Speed Audio Stream Interface 3 (IT 3) alternate 1 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   3                 ;; Zero-based index of this interface
      db   1                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   03H                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   2                    ;; Bytes per sample
      db   16                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   07H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay
      
#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   87H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif      
      
;; High Speed Audio Stream Interface 3 (IT 3) alternate 2 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   3                 ;; Zero-based index of this interface
      db   2                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   03H 	                ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   07H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   87H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif

#ifndef _MODEL_CM6632A_

;; High Speed Audio Stream Interface 3 (IT 3) alternate 3 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   3                 ;; Zero-based index of this interface
      db   3                 ;; Alternate setting
#ifdef _FEEDBACK_            
      db   2                 ;; Number of end points 
#else
      db   1                 ;; Number of end points 
#endif	        
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   03H 	                ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   4                    ;; Bytes per sample
      db   32                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   07H               ;; Endpoint number, and direction
#ifdef _FEEDBACK_      
      db   05H               ;; Endpoint type: ISO & Asynchronous
#else
      db   0DH               ;; Endpoint type: ISO & Synchronous
#endif      
      db   2*4*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*4*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

#ifdef _FEEDBACK_     
;; High Speed Feedback Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   87H               ;; Endpoint number, and direction
      db   00010001b         ;; Endpoint type: ISO & Feedback
      dw   0400H             ;; Maximun packet size: 4
      db   4                 ;; Polling interval: 1 ms            
#endif

#endif

;; High Speed Audio Stream Interface 4 (OT 10) alternate 0 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   4                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   0                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   6                 ;; Interface descriptor string index
 
;; High Speed Audio Stream Interface 4 (OT 10) alternate 1 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   4                 ;; Zero-based index of this interface
      db   1                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   0AH                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                  ;; Format type: Type I
      db   2                  ;; Bytes per sample
      db   16                 ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   89H               ;; Endpoint number, and direction
      db   05H               ;; Endpoint type: ISO & Asynchronous
      db   2*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

;; High Speed Audio Stream Interface 4 (OT 10) alternate 2 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   4                 ;; Zero-based index of this interface
      db   2                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   0AH                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                   ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   89H               ;; Endpoint number, and direction
      db   05H               ;; Endpoint type: ISO & Asynchronous
      db   2*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

;; High Speed Audio Stream Interface 5 (OT 11) alternate 0 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   5                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   0                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   9                 ;; Interface descriptor string index
 
;; High Speed Audio Stream Interface 5 (OT 11) alternate 1 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   5                 ;; Zero-based index of this interface
      db   1                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   0BH                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                 ;; Format type: Type I
      db   2                 ;; Bytes per sample
      db   16                ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   88H               ;; Endpoint number, and direction
      db   05H               ;; Endpoint type: ISO & Asynchronous
      db   2*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

;; High Speed Audio Stream Interface 5 (OT 11) alternate 2 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   5                 ;; Zero-based index of this interface
      db   2                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   0BH                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                    ;; Format type: Type I
      db   3                    ;; Bytes per sample
      db   24                  ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   88H              ;; Endpoint number, and direction
      db   05H               ;; Endpoint type: ISO & Asynchronous
      db   2*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

;; High Speed Audio Stream Interface 6 (OT 12) alternate 0 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   6                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   0                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   7                  ;; Interface descriptor string index
 
;; High Speed Audio Stream Interface 6 (OT 12) alternate 1 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   6                 ;; Zero-based index of this interface
      db   1                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   0CH                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                 ;; Format type: Type I
      db   2                 ;; Bytes per sample
      db   16                ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   8AH               ;; Endpoint number, and direction
      db   05H               ;; Endpoint type: ISO & Asynchronous
      db   2*2*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*2*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

;; High Speed Audio Stream Interface 6 (OT 12) alternate 2 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   6                 ;; Zero-based index of this interface
      db   2                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   01H               ;; Interface class
      db   02H               ;; Interface sub class
      db   20H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed AS General Interface Descriptor
      db   DSCR_AS_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_INTERFACE    ;; Descriptor type
      db   AS_SUBTYPE_GENERAL   ;; AS Descriptor subtype
      db   0CH                  ;; Terminal ID link to
      db   00000101b            ;; Control bitmap
      db   1                    ;; Format type: Type I
      dw   0100H                ;; Data type: PCM
      dw   0000H                ;; Data type
      db   2                    ;; Number of channels
      dw   0300H                ;; Config of logical channels
      dw   0000H                ;; Config of logical channels
      db   0                    ;; String index of first logical channel

;; High Speed AS Format Descriptor
      db   DSCR_AS_FORMAT_LEN;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AS_SUBTYPE_FORMAT ;; AS Descriptor subtype
      db   1                 ;; Format type: Type I
      db   3                 ;; Bytes per sample
      db   24                ;; Bits per sample

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   08AH              ;; Endpoint number, and direction
      db   05H               ;; Endpoint type: ISO & Asynchronous
      db   2*3*(192/8+2) mod 256   ;; Maximun packet size (LSB)
      db   2*3*(192/8+2)  /  256   ;; Maximun packet size (MSB)
      db   01H               ;; Polling interval: 1 micro frame

;; High Speed AS General Endpoint Descriptor
      db   DSCR_EP_GENERAL_LEN_20  ;; Descriptor length
      db   DSCR_CS_ENDPOINT     ;; Descriptor type
      db   EP_SUBTYPE_GENERAL   ;; EP Descriptor subtype
      db   0                    ;; Attribute
      db   0                    ;; Control bitmap
      db   0                    ;; Lock delay unit
      dw   0                    ;; Lock delay

;; High Speed Interface association Descriptor
      db   DSCR_INTRFC_ASSOC_LEN ;; Descriptor length
      db   DSCR_INTRFC_ASSOC     ;; Descriptor type
      db   7                     ;; First interface
      db   1                     ;; Number of interfaces
      db   3                     ;; Function class
      db   0                     ;; Function sub class
      db   00H                   ;; Function protocol
      db   0                     ;; String index
      
;; High Speed HID Interface 7 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   7                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   1                 ;; Number of end points 
      db   03H               ;; Interface class
      db   00H               ;; Interface sub class
      db   00H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index

;; High Speed HID Descriptor
      db   09H               ;; Descriptor length
      db   21H               ;; Descriptor type
      dw   0001              ;; HID version
      db   0                 ;; Country code
      db   1                 ;; Number of descriptors
      db   22H               ;; Descriptor type: Report
      db   (HidReportDscrEnd-HidReportDscr) mod 256 ;; Length of descriptor (LSB)
      db   (HidReportDscrEnd-HidReportDscr)  /  256 ;; Length of descriptor (MSB)

;; High Speed Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   84H               ;; Endpoint number, and direction
      db   ET_INT            ;; Endpoint type
      db   10H               ;; Maximun packet size (LSB) 
      db   00H               ;; Maximun packet size (MSB)
      db   04H               ;; Polling interval

#ifdef _MIDI_
;; High Speed Interface association Descriptor
      db   DSCR_INTRFC_ASSOC_LEN ;; Descriptor length
      db   DSCR_INTRFC_ASSOC     ;; Descriptor type
      db   8                     ;; First interface
      db   2                     ;; Number of interfaces
      db   1                     ;; Function class
      db   0                     ;; Function sub class
      db   00H                   ;; Function protocol
      db   0                     ;; String index
      
;; High Speed Midi Audio Control Interface 8 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   8                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   0                 ;; Number of end points 
      db   01H               ;; Interface class
      db   01H               ;; Interface sub class
      db   00H               ;; Interface sub sub class
      db   12                 ;; Interface descriptor string index

Audio20HsMidiAcInterfaceDscrHeader:
      db   (Audio20HsMidiAcInterfaceDscrHeaderEnd - Audio20HsMidiAcInterfaceDscrHeader) ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   AC_SUBTYPE_HEADER ;; AC Descriptor subtype
      dw   0001H             ;; Audio class release number
      db   (Audio20HsMidiAcInterfaceDscrEnd-Audio20HsMidiAcInterfaceDscrHeader) mod 256 ;; Total Length (LSB)
      db   (Audio20HsMidiAcInterfaceDscrEnd-Audio20HsMidiAcInterfaceDscrHeader)  /  256 ;; Total Length (MSB)
      db   01H               ;; Number of AS interfaces
      db   9                 ;; AS interface number
Audio20HsMidiAcInterfaceDscrHeaderEnd:

Audio20HsMidiAcInterfaceDscrEnd:

;; High Speed MIDI Interface 9 Descriptor
      db   DSCR_INTRFC_LEN   ;; Descriptor length
      db   DSCR_INTRFC       ;; Descriptor type
      db   9                 ;; Zero-based index of this interface
      db   0                 ;; Alternate setting
      db   2                 ;; Number of end points 
      db   01H               ;; Interface class
      db   03H               ;; Interface sub class
      db   00H               ;; Interface sub sub class
      db   0                 ;; Interface descriptor string index
            
Audio20HsMSInterfaceDscrHeader:
      db   7                 ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   1                 ;; MS Descriptor subtype
      dw   0001H             ;; Audio class release number
      db   (Audio20HsMsInterfaceDscrEnd-Audio20HsMsInterfaceDscrHeader) mod 256 ;; Total Length (LSB)
      db   (Audio20HsMsInterfaceDscrEnd-Audio20HsMsInterfaceDscrHeader)  /  256 ;; Total Length (MSB)

;; High Speed MIDI IN Jack Descriptor
      db   6                 ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   2                 ;; AC Descriptor subtype
      db   01H
      db   01H
      db   0

;; High Speed MIDI IN Jack Descriptor
      db   6                 ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   2                 ;; AC Descriptor subtype
      db   02H
      db   02H
      db   0

;; High Speed MIDI OUT Jack Descriptor
      db   9                 ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   3                 ;; AC Descriptor subtype
      db   01H
      db   03H
      db   1
      db   2
      db   1
      db   0

;; High Speed MIDI OUT Jack Descriptor
      db   9                 ;; Descriptor length
      db   DSCR_CS_INTERFACE ;; Descriptor type
      db   3                 ;; AC Descriptor subtype
      db   02H
      db   04H
      db   1
      db   1
      db   1
      db   0

;; High Speed Bulk Out Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   01H               ;; Endpoint number, and direction
      db   ET_BULK           ;; Endpoint type: BULK  
      dw   4000H             ;; Maximun packet size
      db   00H               ;; Polling interval

;; High Speed MS General Endpoint Descriptor
      db   5                 ;; Descriptor length
      db   DSCR_CS_ENDPOINT  ;; Descriptor type
      db   1                 ;; EP Descriptor subtype
      db   01H               ;; Attribute
      db   01H

;; High Speed Bulk In Endpoint Descriptor
      db   DSCR_ENDPNT_LEN   ;; Descriptor length
      db   DSCR_ENDPNT       ;; Descriptor type
      db   82H               ;; Endpoint number, and direction
      db   ET_BULK           ;; Endpoint type: BULK	
      dw   4000H             ;; Maximun packet size
      db   00H               ;; Polling interval

;; High Speed MS General Endpoint Descriptor
      db   5                 ;; Descriptor length
      db   DSCR_CS_ENDPOINT  ;; Descriptor type
      db   1                 ;; EP Descriptor subtype
      db   01H               ;; Attribute
      db   03H

Audio20HsMsInterfaceDscrEnd:

#endif

Audio20HighSpeedConfigDscrEnd:

